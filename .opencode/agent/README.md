# Agents

Adicione agents aqui no formato `.opencode/agent/<nome>.md`.

Cada agent precisa de:
- `description`
- `mode` (primary, subagent, all)
- `permission` explícitas

Consulte o GUIA-OPENCODE-AGENTS-SKILLS.md para detalhes.
